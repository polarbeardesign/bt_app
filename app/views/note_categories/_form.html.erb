<%= form_for(@note_category) do |f| %>
  <% if @note_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note_category.errors.count, "error") %> prohibited this note_category from being saved:</h2>

      <ul>
      <% @note_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
